# Software Bill of Materials

Replace this file with a generated `cyclonedx.json` covering every JAR packaged
under `lib/`. Generate it from the locked build inputs used to produce the final
connector archive.
